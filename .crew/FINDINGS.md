# Devon's Output
_Generated by Devon (qwen3.5:35b-a3b) in 145.8s, 4000 tokens_

## Brief
# BRIEF: Add Brett + Chef tabs to Kitchen dashboard

## Context
The Kitchen dashboard lives at:
`/home/openclaw-agent/.openclaw/workspace/canvas/kitchen/index.html`

It already has a working Bob tab. Your job is to ADD two more tabs to the existing file using the EXACT same pattern. Do NOT rewrite the file. Output ONLY the code snippets to add, clearly labelled.

## What Bob's tab looks like (pattern to follow)
In the HTML, tabs are added here:
```html
<div class="ref-tab" data-ref="bob" id="bobTab">🔨 Bob</div>
```
And panes here:
```html
<div class="ref-pane" data-refpane="bob" id="bobPane" style="padding:0;"></div>
```
The JS is a self-invoking function at the bottom of the `<script>` block, before `</script>`.

## Output format
Output THREE clearly labelled sections — nothing else:

### SECTION 1: TAB HTML (paste after the Bob tab line)
Two lines of HTML for the two new tab buttons.

### SECTION 2: PANE HTML (paste after the Bob pane line)
Two lines of HTML for the two new pane divs.

### SECTION 3: JAVASCRIPT (paste before `</script>`)
Two self-invoking dashboard functions — one for Brett, one for Chef. Follow the Bob dashboard pattern exactly (same CSS var names, same poll-on-tab-click pattern, same log colorizer).

## Brett Tab (`data-ref="brett"`, `data-refpane="brett"`, id `brettTab`/`brettPane`)
Label: `⚙️ Brett`
Poll every 4s when active. Stop when tab switches.
Log URLs (already symlinked, served via kitchen server):
- `http://localhost:7749/canvas/kitchen/tmp-proxy/crew-brief.md`
- `http://localhost:7749/canvas/kitchen/tmp-proxy/crew-review.md`
- `http://localhost:7749/canvas/kitchen/tmp-proxy/crew-cycle-log.md`

UI sections:
1. **Header**: status dot (green + "Reviewing" if review.md fetched OK, grey + "Idle" if 404/error) + refresh button
2. **Current Brief**: `<pre>` block showing crew-brief.md, max-height 180px, scrollable
3. **Last Review**: `<pre>` block showing crew-review.md, color first line green if starts with PASS, red if FAIL
4. **Cycle Log**: last 30 lines of crew-cycle-log.md, same line coloring as Bob (green=PASS, red=FAIL, gold=milestone)

## Chef Tab (`data-ref="chef"`, `data-refpane="chef"`, id `chefTab`/`chefPane`)
Label: `🍳 Chef`
Poll every 4s when active.
Log URLs:
- `http://localhost:7749/canvas/kitchen/tmp-proxy/chef-heartbeat.log`
- `http://localhost:7749/canvas/kitchen/tmp-proxy/chef-cron.log`

UI sections:
1. **Header**: always green dot + "Active"
2. **Heartbeat**: last 20 lines of chef-heartbeat.log, or placeholder "No heartbeat log yet"
3. **Cron**: last 20 lines of chef-cron.log, or placeholder "No cron log yet"

## CSS
Reuse ALL existing CSS variables (`--bg`, `--bg-card`, `--border`, `--text`, `--gold`, `--emerald`, `--red`, etc.)
Reuse `.bob-header`, `.bob-btn`, `.bob-log-scroll`, `.bob-log-line`, `.bob-no-data` styles where possible.
Add only what's needed for Brett/Chef specifics.

## Constraints
- Output the three sections ONLY — no explanations, no full file
- Use the exact same self-invoking function pattern as the Bob dashboard
- Poll only when tab is active, clearInterval when switching away
- No new servers, no external APIs
- Handle 404/network errors gracefully (show placeholder, grey dot)

## Code Written
```

```
[Full output written to target file]
