# AutoResearch Run — 2026-05-15 18:49 UTC

## Hypothesis
Moroccan users reporting ~3 minute Maghrib drift are seeing the public no-options path auto-apply city-registry elevation, while official Habous/Mawaqit Moroccan timetables are uniform city/region tables. The existing Habous monthly fixture gate passed because it explicitly supplied `elevation: 0`, masking the public-path drift.

## Wiki sources consulted
- `knowledge/wiki/regions/morocco.md` — Morocco follows the official Habous city/region timetable stance.
- `knowledge/wiki/corrections/elevation.md` — elevation correction is institution-dependent; not every official city timetable applies observer-elevation horizon correction.

## Change made
- In `src/engine.js`, added Morocco to a uniform-city-timetable elevation policy: when the caller is silent on elevation, Morocco defaults to `elevationSource: 'country-uniform-timetable'`, `elevation: 0`, and no auto elevation correction.
- Caller-explicit elevation still applies normally for users/apps that intentionally want observer-elevation correction.
- Fixed `methodFromString('Morocco')` so explicit `method: 'Morocco'` preserves the canonical +5 Dhuhr / +5 Maghrib Path A buffers instead of returning bare 19°/17° times.
- Updated `test/habousMoroccoFixture.test.js` to validate the real public default instead of forcing `elevation: 0`.
- Updated `scripts/validate-habous-morocco.js` to gate the five prayer times separately from Shuruq, matching the fixture policy that Shuruq is a loose source-sanity signal.
- Updated README, changelog, source/disagreement docs, downstream guidance, and the TypeScript `ElevationSource` declaration with the Morocco uniform-timetable stance.

## Before WMAE
Train WMAE 0.9757. Holdout WMAE 7.3646.

## After WMAE
Train WMAE 0.9757. Holdout WMAE 7.3646.

`node eval/compare.js` reports a wash and therefore fails the strict autoresearch ratchet. This is expected: the eval fixtures already pass `elevation: 0`, so the ratchet does not exercise the public no-options path that Moroccan users actually hit. This change is a public API bug fix and release blocker, not an eval-fitting correction.

## Morocco live-source validation
`node scripts/validate-habous-morocco.js --allow-insecure-habous-cert` on 2026-05-15:

- 33 mapped Moroccan cities checked against Habous live current-day endpoint.
- Five prayer times all within 3 minutes.
- Shuruq within the loose 10 minute source-sanity envelope.
- Maghrib moved from the previous +1..+4 minute late public-path pattern to -3..+2 minutes after removing auto elevation from the Morocco default.

## Verification
- `npx vitest run test/engine.test.js test/habousMoroccoFixture.test.js test/publicApi.test.js test/prayerTimesLocation.test.js` — 144 tests passed
- `npm test` — 25 files / 445 tests passed
- `node scripts/validate-habous-morocco.js --allow-insecure-habous-cert` — passed
- `node eval/eval.js` — train 0.9757, holdout 7.3646
- `node eval/compare.js` — expected wash failure; ratchet blind spot documented above
- `npm run build:charts` — ran after the engine change; generated run/chart noise was not committed because WMAE was unchanged and the only diff came from duplicate local eval rows.

## Verdict
ACCEPTED WITH HUMAN-RELEASE EXCEPTION — the strict train-WMAE ratchet is unchanged because existing fixtures already encoded uniform elevation. The public Morocco default was wrong for official Habous uniform-city tables; the live validator and updated fixture gate now cover that path.

## Scholarly classification
🟡→🟢 Approaching established — this narrows elevation application to match the official Moroccan Habous/Mawaqit city-timetable stance. It does not alter Fajr/Isha angles, Dhuhr/Maghrib Path A buffers, or the wasail/ibadat boundary.
