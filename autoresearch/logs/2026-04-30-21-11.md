# AutoResearch Run — 2026-04-30 21:11 UTC

## Hypothesis (initial)

Two candidates surfaced from the new per-source view of `eval/eval.js`:

1. **JAKIM Fajr bias.** Engine matches the Aladhan-Singapore method (20°/18°) exactly, but the Malaysian official source (JAKIM via waktusolat.app) shows engine Fajr is **−8.70 min** of JAKIM's published Fajr — i.e., calc is 8.7 minutes *earlier* than what Malaysian users see in their official tables. That is the **ihtiyat-unsafe** direction. Hypothesis: JAKIM applies an unstated precaution offset (or uses a fitting that diverges from pure 20° depression).

2. **Morocco Fajr bias.** Engine matches the formal Ministry of Habous method (Fajr 18°) per `wiki/methods/morocco.md`, but `eval/data/train/morocco.json` shows Casablanca and Rabat with Fajr bias **+5.00 / +5.10 min** (calc later than ground truth). The bias direction is *safe* (longer night = more cautious for Fajr), but a 5-minute systematic offset is unusually large for an institutional method match.

## Wiki sources consulted

- `knowledge/wiki/methods/overview.md` — Method comparison table; documents JAKIM as 20°/18°, no offset noted. Documents Morocco as 18°/17° formal Ministry method but ground truth fixture row at line 118 is **Custom 99 (19°/17°)**.
- `knowledge/wiki/methods/morocco.md` — Authoritative Morocco method page. Confirms Ministry's stated method is 18°/17°. Line 117 of methods/overview echoed in this page: ground truth was fetched with 19°/17° as "community calibration that best reproduces the Ministry's timetable."
- `knowledge/wiki/regions/morocco.md` — Regional notes. Line 90: *"For coastal cities (Casablanca, Rabat, Agadir), the 18°/17° formula applied at the correct coordinates and UTC offset closely matches the Ministry timetable, typically within 1–2 minutes."*
- *(JAKIM)* — searched; no `knowledge/wiki/regions/malaysia.md` and no `knowledge/wiki/methods/jakim.md` exist. Methods/overview.md only states 20°/18°, no offset documentation.

## Findings

### Finding 1: JAKIM Fajr −8.7 min — knowledge gap, NOT actioned

The wiki does not document any JAKIM-specific adjustment beyond the 20°/18° angles. Per `autoresearch/prompt.md` rule:

> "If you cannot find a wiki page that supports the correction, do not make the correction. The wiki is the knowledge boundary — do not reason beyond it."

A correction that adds a Malaysia-specific Fajr ihtiyat offset (e.g. +8 min for Malaysia bounding box) cannot be deployed without wiki documentation of JAKIM's actual published practice. The bias is real, the regression direction is unsafe (calc earlier than what Malaysian users pray to), but engine modification is **blocked on knowledge base update**.

**Recommended wiki additions (human-driven, not autoresearch):**

- `knowledge/wiki/regions/malaysia.md` — JAKIM topographic elevation correction practice; any documented Fajr/Isha precaution offsets; relationship between e-solat.gov.my published times and the bare 20° formula.
- `knowledge/wiki/methods/jakim.md` — official JAKIM method page distinct from the Singapore method (currently the engine maps Malaysia → `adhan.CalculationMethod.Singapore()`).

Until those exist, the engine should NOT be modified. The −8.7 min bias is documented as a known disagreement with the Malaysian official source.

### Finding 2: Morocco Fajr +5 min — likely a ground-truth fetch artifact

Cross-referencing the wiki:

| Source | Method used | Fajr at Casablanca April 1 |
|---|---|---|
| `eval/data/train/morocco.json` | Aladhan custom 99 with **Fajr=19°** | ground truth: 05:08 |
| Engine (`src/engine.js`) | adhan.js Other() with Fajr=18° | calc: ~05:13 |
| Ministry timetable (per wiki) | Stated 18°/17°, "matches Ministry within 1-2 min" | ~05:13 |

The wiki itself documents that the formal Ministry method is 18° AND that 18° matches Ministry within 1-2 min. The 19° "community calibration" was an unstated decision when the eval data was collected; it does not match the engine's stated Ministry method nor the wiki's stated Ministry behavior.

**The +5 min bias is an artifact of the Aladhan fetch parameter, not an engine error.**

This means:
- Engine for Morocco is correct relative to its stated authority.
- Ground truth for Morocco diverges from its stated authority by ~5 min for Fajr (because of the 19° fetch).
- "Improving" the engine to match this ground truth would push it AWAY from the actual Ministry timetable — that is exactly the failure mode the per-source view is designed to detect.

**Recommended action (human-driven):** refetch `eval/data/train/morocco.json` using the formal Ministry parameters (method=99, methodSettings=`18,null,17`). This modifies ground truth, which CLAUDE.md restricts; treat as a knowledge-base maintenance task. Add Morocco to `scripts/fetch-aladhan.js` CITIES with the correct settings.

The deeper fix — adding a non-Aladhan Habous source (e.g., Mawaqit, which covers Moroccan mosques) — is queued separately as part of source-corpus expansion.

## Change made

**None.** This is a research-only run. No edits to `src/engine.js`.

The reasoning trail above is the deliverable.

## Before / after WMAE

N/A — no engine change. Latest baseline (per `eval/results/runs.jsonl`):

- Train WMAE: 1.3296
- Holdout WMAE: 6.2818

## Verdict

**RESEARCH-ONLY — both findings deferred pending knowledge-base updates.**

JAKIM offset blocked on missing wiki documentation. Morocco bias blocked on potentially-incorrect ground truth that requires human approval to modify (per CLAUDE.md "ground truth is sacred").

## Scholarly classification

N/A — no correction deployed. If/when a JAKIM offset is documented in the wiki, a future run could test it as 🟡 (Limited precedent — JAKIM-specific) and ratchet it. If/when Morocco ground truth is refetched with the formal 18° angle, the engine should already match within 1-2 min per the wiki's expectation.

## Follow-ups queued

- Add `knowledge/wiki/regions/malaysia.md` documenting JAKIM published-vs-formula divergence.
- Add Morocco to `scripts/fetch-aladhan.js` with formal `18,null,17` to refetch its ground truth.
- Add Mawaqit fetch adapter to bring non-Aladhan Moroccan ground truth into the corpus.
