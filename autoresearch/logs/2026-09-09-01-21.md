# Correctness follow-up — 2026-09-09 01:21 UTC

## Hypothesis
The downstream polar screen incorrectly combines a missing Fajr with an advisory
whose code says a rule was applied, and describes a TwilightAngle result using
the MiddleOfTheNight note. Scope diagnostic output to the result and selected
rule without changing calculations.

## Wiki sources consulted
- knowledge/wiki/regions/iceland.md — the narrow-gap note specifically describes MiddleOfTheNight; it is not a general explanation for unavailable TwilightAngle results.
- knowledge/wiki/regions/high-latitude.md — availability guidance and distinct regional rules (existing references retained).

## Change made
In `src/validity.js`, emit each high-latitude advisory only when that prayer has a
finite result. Critical unavailability warnings remain. In `src/engine.js`, emit
the existing middle-of-night note only for that configured rule with finite Fajr
and Isha results. No new time, fallback method or scholarly ruling is introduced.

## Before / after WMAE
| Split | Before (min) | After (min) |
|---|---:|---:|
| Train | 0.9757475083056478 | 0.9757475083056478 |
| Holdout | 7.36457344701456 | 7.36457344701456 |

Every train/holdout metric, city/source cell and per-prayer signed bias is exactly
identical. All regional and bias deltas are zero.

## Validation
The regression tests first reproduced three failures. The repaired suite passes
505 tests. The actual downstream app boundary passes 168 checks, including
absence of the misleading applied-rule warning and middle-of-night note in the
reported Sweden case. `npm run regenerate-docs` completed; curated numeric claims
were audited and remain unchanged.

## Verdict
ACCEPTED as a user-authorized correctness repair in draft PR #191 using
`scripts/check-eval-change.js ... unchanged`, which passed exact train and
holdout metric identity. The standard `eval/compare.js` was also run and rejected
the WMAE tie as required. This is not an accepted calibration or an accuracy gain.

## Scholarly classification
🟢 Established — diagnostic scoping only. Existing method choices, prayer times,
ihtiyat rounding and guidance for valid MiddleOfTheNight results are preserved.
